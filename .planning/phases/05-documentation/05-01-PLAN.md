---
phase: 05-documentation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [README.md]
autonomous: true
requirements: [DOCS-01]
user_setup: []

must_haves:
  truths:
    - "User sees security warning about agent forwarding risks on untrusted hosts"
    - "User sees brief usage examples for common agent forwarding scenarios"
  artifacts:
    - path: "README.md"
      provides: "Security warning and usage guidance for agent forwarding"
      contains: "### Agent Forwarding Security"
  key_links:
    - from: "README.md"
      to: "User understanding"
      via: "markdown rendering"
      pattern: "### Agent Forwarding Security"
---

<objective>
Add security warning and usage examples for SSH agent forwarding to README.md.

Purpose: Help users understand the security risks of agent forwarding on untrusted hosts and provide guidance on when to use it.
Output: Updated README with Agent Forwarding Security subheading and usage examples.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-documentation/05-CONTEXT.md
@.planning/phases/05-documentation/05-RESEARCH.md
</context>

<interfaces>
<!-- No code interfaces - documentation only -->
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Add Agent Forwarding Security subheading and warning</name>
  <files>README.md</files>
  <action>
    Add a new "### Agent Forwarding Security" subheading under the existing "## Security" section (after line 87, before "### Default Blacklist").

    Add the following content under this subheading:

    ```markdown
    ### Agent Forwarding Security

    Only use `forwardAgent` on trusted hosts. Root users on remote systems can access your forwarded agent socket and authenticate to other servers using your credentials.

    ### Agent Forwarding Usage

    Use agent forwarding when you need to authenticate from a remote host using your local SSH keys:

    - **SCP/rsync hops**: Transfer files between remote servers without copying private keys. Your local key authenticates both connections.
    - **Git clone on remote**: Clone private repositories on a jump host using your local credentials.
    ```

    CRITICAL: Follow the user's locked decisions exactly:
    - Brief admonition only (1-2 sentences for security warning)
    - Do NOT add technical deep-dive about socket hijacking mechanics
    - Do NOT write full command walkthroughs - keep examples as short descriptions + benefits
  </action>
  <verify>
    <automated>grep -q "### Agent Forwarding Security" README.md && grep -q "forwardAgent on trusted hosts" README.md && grep -q "SCP/rsync hops" README.md</automated>
  </verify>
  <done>
    README contains "### Agent Forwarding Security" subheading under Security section with brief security warning and usage examples for SCP/rsync hops and git clone.
  </done>
</task>

<task type="auto">
  <name>Task 2: Verify documentation builds cleanly</name>
  <files>README.md</files>
  <action>
    Run the build and tests to ensure no regressions were introduced. Documentation changes should not affect code.
  </action>
  <verify>
    <automated>bun test && bun run build</automated>
  </verify>
  <done>
    All tests pass and TypeScript compiles without errors.
  </done>
</task>

</tasks>

<verification>
- [ ] README contains "### Agent Forwarding Security" subheading
- [ ] Security warning mentions trusted hosts and root socket access
- [ ] Usage examples include SCP/rsync hops scenario
- [ ] Build and tests pass with no regressions
</verification>

<success_criteria>
- User can find agent forwarding security guidance in README
- Warning is prominent but not alarmist (brief admonition)
- Examples are concise and demonstrate common use cases
</success_criteria>

<output>
After completion, create `.planning/phases/05-documentation/05-01-SUMMARY.md`
</output>
