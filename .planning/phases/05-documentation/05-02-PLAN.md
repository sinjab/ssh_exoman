---
phase: 05-documentation
plan: 02
type: execute
wave: 1
depends_on: []
files_modified: [src/prompts/help.ts]
autonomous: true
requirements: [DOCS-02]
user_setup: []

must_haves:
  truths:
    - "User gets guidance from ssh_help prompt on forwardAgent parameter"
    - "User sees warning to only enable forwardAgent on trusted hosts"
  artifacts:
    - path: "src/prompts/help.ts"
      provides: "Guidance for forwardAgent parameter in ssh_help prompt"
      contains: "forwardAgent (boolean, optional, default: false)"
  key_links:
    - from: "src/prompts/help.ts"
      to: "MCP prompt response"
      via: "Claude Desktop prompt system"
      pattern: "forwardAgent.*Forward local SSH agent"
---

<objective>
Add forwardAgent parameter guidance to the ssh_help MCP prompt.

Purpose: Provide Claude and users with context on the forwardAgent parameter when they request help via the ssh_help prompt.
Output: Updated ssh_help prompt with forwardAgent parameter description.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-documentation/05-CONTEXT.md
@.planning/phases/05-documentation/05-RESEARCH.md
</context>

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->

From src/prompts/help.ts (existing format):
```typescript
1. **execute_command** - Run a command on a remote host via SSH
   - Parameters: host (string), command (string), timeout (number, optional)
```

The forwardAgent parameter should be appended to the execute_command parameter list using the same format.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Add forwardAgent parameter to execute_command in ssh_help prompt</name>
  <files>src/prompts/help.ts</files>
  <action>
    Update line 42 in src/prompts/help.ts to add the forwardAgent parameter to the execute_command tool description.

    Current format:
    ```typescript
    1. **execute_command** - Run a command on a remote host via SSH
       - Parameters: host (string), command (string), timeout (number, optional)
    ```

    Change to:
    ```typescript
    1. **execute_command** - Run a command on a remote host via SSH
       - Parameters: host (string), command (string), timeout (number, optional), forwardAgent (boolean, optional, default: false) - Forward local SSH agent to remote host. Allows remote commands to authenticate with other SSH servers using your local keys. Only enable on fully trusted hosts.
    ```

    CRITICAL: Use the exact text from the user's locked decision:
    - "forwardAgent (boolean, optional, default: false) - Forward local SSH agent to remote host. Allows remote commands to authenticate with other SSH servers using your local keys. Only enable on fully trusted hosts."
    - Do NOT create a separate guidance section
    - Do NOT add more detail than specified
  </action>
  <verify>
    <automated>grep -q 'forwardAgent (boolean, optional, default: false)' src/prompts/help.ts && grep -q 'Only enable on fully trusted hosts' src/prompts/help.ts</automated>
  </verify>
  <done>
    ssh_help prompt includes forwardAgent parameter in execute_command with description matching the locked decision text.
  </done>
</task>

<task type="auto">
  <name>Task 2: Verify prompt compiles and tests pass</name>
  <files>src/prompts/help.ts</files>
  <action>
    Run the build and tests to ensure the prompt change compiles and no regressions were introduced.
  </action>
  <verify>
    <automated>bun test && bun run build</automated>
  </verify>
  <done>
    All tests pass and TypeScript compiles without errors.
  </done>
</task>

</tasks>

<verification>
- [ ] ssh_help prompt includes forwardAgent in execute_command parameters
- [ ] Parameter format matches existing parameter style
- [ ] Warning about trusted hosts is included
- [ ] Build and tests pass with no regressions
</verification>

<success_criteria>
- Claude Desktop users get guidance on forwardAgent when requesting ssh_help prompt
- Parameter description is concise and includes security warning
- Format is consistent with other parameter descriptions
</success_criteria>

<output>
After completion, create `.planning/phases/05-documentation/05-02-SUMMARY.md`
</output>
