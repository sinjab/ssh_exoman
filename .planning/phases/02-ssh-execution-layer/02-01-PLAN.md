---
phase: 02-ssh-execution-layer
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/ssh/config-parser.ts
  - src/ssh/config-parser.test.ts
  - src/ssh/command-detection.ts
  - src/ssh/command-detection.test.ts
  - src/ssh/index.ts
autonomous: true
requirements:
  - SSH-01
  - SSH-03
user_setup: []

must_haves:
  truths:
    - "Server resolves host aliases from ~/.ssh/config"
    - "Host resolution returns HostName, User, Port, IdentityFile fields"
    - "Missing fields default to User=$USER, Port=22"
    - "Server correctly detects simple vs complex commands"
    - "Complex commands include pipes, redirects, semicolons, globs, variables"
    - "All commands are wrapped in /bin/sh -c for predictable behavior"
  artifacts:
    - path: "src/ssh/config-parser.ts"
      provides: "SSH config parsing and host resolution"
      exports: ["parseSSHConfig", "resolveHost", "listHosts", "HostConfig"]
    - path: "src/ssh/command-detection.ts"
      provides: "Command complexity detection and wrapping"
      exports: ["isComplexCommand", "wrapCommand"]
    - path: "src/ssh/index.ts"
      provides: "Barrel exports for SSH module"
  key_links:
    - from: "src/ssh/config-parser.ts"
      to: "~/.ssh/config"
      via: "fs.readFileSync with ssh-config library"
      pattern: "SSHConfig\\.parse"
    - from: "src/ssh/command-detection.ts"
      to: "command string"
      via: "regex test for shell metacharacters"
      pattern: "SHELL_META_CHARS\\.test"
---

<objective>
Implement SSH config parsing with host resolution and command complexity detection for routing execution.

Purpose: Enable the server to resolve host aliases from ~/.ssh/config and detect whether commands need shell wrapping for complex constructs (pipes, redirects, etc).
Output: SSH config parser module, command detection module, barrel exports
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-ssh-execution-layer/02-CONTEXT.md
@.planning/phases/02-ssh-execution-layer/02-RESEARCH.md

<interfaces>
<!-- Key types from existing codebase that executor needs. -->

From src/types.ts:
```typescript
export type Result<T> =
  | { success: true; data: T }
  | { success: false; error: { code: string; message: string } };
```

From src/errors.ts:
```typescript
export enum ErrorCode {
  CONFIG_ERROR = "CONFIG_ERROR",
  SSH_CONNECTION_FAILED = "SSH_CONNECTION_FAILED",
  // ...
}
export function errorResult<T>(code: ErrorCode, message: string): Result<T>;
```

From src/config.ts:
```typescript
export interface AppConfig {
  securityMode: SecurityMode;
  sshConnectTimeout: number;
  commandTimeout: number;
  logLevel: LogLevel;
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create SSH config parser module</name>
  <files>src/ssh/config-parser.ts, src/ssh/config-parser.test.ts</files>
  <behavior>
    - Test 1: parseSSHConfig returns SSHConfig object for valid config file
    - Test 2: parseSSHConfig returns empty SSHConfig when file does not exist
    - Test 3: resolveHost returns HostConfig with merged values from compute()
    - Test 4: resolveHost returns null for unknown host
    - Test 5: resolveHost applies defaults (User=$USER, Port=22) for missing fields
    - Test 6: listHosts filters out wildcard patterns (*, *.example.com)
  </behavior>
  <action>
    Create `src/ssh/config-parser.ts` that:

    1. Imports `SSHConfig` from `ssh-config` library
    2. Defines `HostConfig` interface with: host, hostname?, user, port, identityFile?
    3. Implements `parseSSHConfig()`: reads ~/.ssh/config, returns SSHConfig object (empty if missing)
    4. Implements `resolveHost(alias, config)`: uses `config.compute(alias)` to get merged config, returns HostConfig with defaults applied
    5. Implements `listHosts(config)`: extracts non-wildcard host entries

    Use the patterns from RESEARCH.md Pattern 1.

    User decisions to honor:
    - Expand wildcard patterns via `.compute()` method
    - Parse standard fields: Host, HostName, User, Port, IdentityFile
    - Return partial config with defaults for missing fields (User=$USER, Port=22)
    - Skip wildcard patterns in listHosts

    Install dependency: `bun add ssh-config`
  </action>
  <verify>
    <automated>bun test src/ssh/config-parser.test.ts</automated>
  </verify>
  <done>SSH config parser correctly resolves host aliases with defaults, lists non-wildcard hosts</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create command detection module</name>
  <files>src/ssh/command-detection.ts, src/ssh/command-detection.test.ts</files>
  <behavior>
    - Test 1: isComplexCommand returns false for simple command "ls -la"
    - Test 2: isComplexCommand returns true for pipe "cat file | grep foo"
    - Test 3: isComplexCommand returns true for redirect "echo hello > file"
    - Test 4: isComplexCommand returns true for semicolon "cmd1; cmd2"
    - Test 5: isComplexCommand returns true for variable "echo $HOME"
    - Test 6: isComplexCommand returns true for subshell "$(command)"
    - Test 7: isComplexCommand returns true for glob "cat *.txt"
    - Test 8: wrapCommand wraps simple command in /bin/sh -c
    - Test 9: wrapCommand escapes double quotes in command
  </behavior>
  <action>
    Create `src/ssh/command-detection.ts` that:

    1. Defines `SHELL_META_CHARS` regex: /[|;&<>\$`\\"'*?[\]{}()]/
    2. Implements `isComplexCommand(command)`: tests for shell metacharacters, &&, ||, $(), backticks
    3. Implements `wrapCommand(command)`: wraps in `/bin/sh -c "{escaped}"` where double quotes are escaped

    User decisions to honor:
    - Always wrap commands in shell (`/bin/sh -c "{command}"`)
    - Shell wrapper ensures consistent behavior with pipes, semicolons, globbing, redirection

    Use the patterns from RESEARCH.md Pattern 3.
  </action>
  <verify>
    <automated>bun test src/ssh/command-detection.test.ts</automated>
  </verify>
  <done>Command detection correctly identifies simple vs complex commands, wrapCommand produces shell wrapper</done>
</task>

<task type="auto">
  <name>Task 3: Create SSH module barrel exports</name>
  <files>src/ssh/index.ts</files>
  <action>
    Create `src/ssh/index.ts` that exports all public APIs from the SSH module:
    - From config-parser: HostConfig, parseSSHConfig, resolveHost, listHosts
    - From command-detection: isComplexCommand, wrapCommand

    Follow the barrel export pattern established in `src/index.ts`.
  </action>
  <verify>
    <automated>bun test src/ssh/config-parser.test.ts src/ssh/command-detection.test.ts</automated>
  </verify>
  <done>SSH module has clean barrel exports following project conventions</done>
</task>

</tasks>

<verification>
- [ ] `bun test src/ssh/config-parser.test.ts` passes
- [ ] `bun test src/ssh/command-detection.test.ts` passes
- [ ] `bun test` (full suite) passes
- [ ] ssh-config dependency installed
</verification>

<success_criteria>
- SSH config parser resolves host aliases from ~/.ssh/config
- Missing config fields default to User=$USER, Port=22
- Command detection identifies pipes, redirects, semicolons, globs, variables
- All commands can be wrapped in /bin/sh -c for predictable execution
</success_criteria>

<output>
After completion, create `.planning/phases/02-ssh-execution-layer/02-01-SUMMARY.md`
</output>
